{
  "name": "cakeboxes-server",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc --noEmit && esbuild src/server.ts --bundle --platform=node --format=esm --outfile=dist/main.js",
    "start": "node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.5",
    "typescript": "^5.5.4"
  }
}
